# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

DATABASE_URL="postgresql://postgres:postgres@frontend-masters-api-design-nodejs-v4-db-1:5432/postgres?schema=public"

# I've decided against pointing at the laravel db. It's technically all event sourced so I can just use my own db
# DATABASE_URL="postgresql://sail:pgsql@localhost:5432/marvel_snap_api?schema=public"

# BTTODO - Does Prisma allow connecting via specifying the connection stirng in chunks? e.g. the password could do
#  with being stored in a secrets manager, but I guess the whole string could be too